import 'package:flutter/material.dart';

class AnimesScreen extends StatelessWidget {
  const AnimesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text('Animes Screen'),
    );
  }
}